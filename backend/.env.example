# Copy to .env and fill in real values. Never commit .env itself.
# Shared across all backend apps when run locally from this directory (each app's ConfigModule
# reads the same file). No PORT here on purpose — each app defaults to its own port in code
# (gateway: 8000, auth: 8001) so running two apps locally at once doesn't collide; override
# with a real shell env var (`PORT=9000 npm run start:dev`) if you need something else.

# Shared JWT signing secret, via @app/auth-kernel. Gateway verifies; Auth Service signs +
# verifies. Per docs/specs/auth.md.
JWT_SECRET=change-me-to-a-long-random-value

# Password hashing pepper — Auth Service only. Per docs/specs/auth.md: never stored in the
# database, must stay identical across restarts or every existing password stops verifying.
PASSWORD_PEPPER=change-me-to-a-different-long-random-value

# Shared Postgres connection, used by every service that owns Postgres tables. This default
# targets the `postgres` service in devops/docker-compose.yml when port-forwarded to localhost;
# docker-compose overrides this to the in-network hostname for container-to-container calls.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/askmycrawl

# First-admin bootstrap (Auth Service only) — see docs/specs/auth.md. Leave both unset to skip;
# safe to leave set permanently, it's a no-op once any admin exists.
ADMIN_EMAIL=admin@gmail.com
ADMIN_PASSWORD=admin

# Gateway only — where it proxies /auth/*, /me, /admin/users* to (see docs/specs/services.md).
# Defaults to http://auth:8001 (the container service name) if unset; override for local
# (non-Docker) `npx nest start gateway --watch`, where that hostname won't resolve.
# AUTH_SERVICE_URL=http://localhost:8001

# Scraper AND Indexer — Redis connection, one shared instance, not one per service (see the devops
# agent's "reuse existing shared infrastructure" rule). Backs BullMQ's process-url/index-page
# queues plus the per-job coordination keys (docs/planning/03-crawler-scraper-indexing-plan.md).
# This default targets devops/redis/docker-compose.yml's published host port; docker-compose
# overrides it to redis:6379 for container-to-container calls.
REDIS_URL=redis://localhost:6379

# Scraper AND Indexer — SeaweedFS's S3-compatible API: the Scraper writes raw scraped HTML here,
# the Indexer only reads it back. Also read directly by devops/seaweedfs/docker-compose.yml itself
# (via env_file) to generate its own S3 identity config at container startup — every reader/writer
# must always agree on the same credentials, so there's exactly one place these are set. This
# default targets the published host port; docker-compose overrides SEAWEEDFS_ENDPOINT to
# http://seaweedfs:8333 for container-to-container calls (the access/secret key stay the same in
# both cases).
SEAWEEDFS_ENDPOINT=http://localhost:8333
SEAWEEDFS_ACCESS_KEY=change-me-to-a-long-random-value
SEAWEEDFS_SECRET_KEY=change-me-to-a-different-long-random-value
SEAWEEDFS_BUCKET=askmycrawl-raw-html

# Scraper only — max fetch attempts per URL for transient failures (timeout/connection error/5xx)
# before it's given up on and added to the job's failed set; a permanent failure (4xx) never
# retries regardless of this value. See docs/planning/03-crawler-scraper-indexing-plan.md §5.
SCRAPER_FETCH_MAX_ATTEMPTS=3

# Indexer only — max index attempts per page for transient failures (connection error, timeout,
# 5xx) before the page is given up on; a permanent failure (missing blob, unparseable HTML, a
# persistent embedding-dimension mismatch) never retries regardless of this value. Mirrors
# SCRAPER_FETCH_MAX_ATTEMPTS's role for the index-page queue.
INDEXER_MAX_ATTEMPTS=3

# Indexer only — embedding provider, spoken via the OpenAI-compatible /v1/embeddings API.
# Provider-agnostic: point EMBEDDING_BASE_URL at any server that speaks that API and nothing else
# changes. EMBEDDING_DIMENSION must match whatever model/provider is actually behind it, or every
# embed call permanently fails. Current default below is a local LM Studio instance — swap freely.
EMBEDDING_BASE_URL=http://localhost:1234/v1
EMBEDDING_MODEL=text-embedding-nomic-embed-text-v1.5
EMBEDDING_DIMENSION=768
# EMBEDDING_API_KEY=

# Indexer only — vector DB connection (currently Qdrant, see devops/qdrant/docker-compose.yml).
# VECTOR_DB_COLLECTION defaults to askmycrawl_chunks in code if unset.
VECTOR_DB_URL=http://localhost:6333
# VECTOR_DB_COLLECTION=askmycrawl_chunks

NODE_ENV=development

# OpenTelemetry — see backend/libs/otel. OTEL_SERVICE_NAME must be unique per app and match the
# name startOtel(...) is called with in that app's main.ts (gateway/auth). Left unset, it falls
# back to "unknown_service"/the value baked into the code — either works, but an explicit env var
# is what devops/docker-compose.yml sets, so keeping the same shape locally avoids surprises.
#
# OTEL_EXPORTER_OTLP_ENDPOINT defaults to http://otel-collector:4317, which only resolves inside
# the Docker Compose network. Running an app locally (npx nest start <app> --watch, not compose) —
# either set this to http://localhost:4317 (with devops/observability's ports published, which
# they are by default) or expect telemetry export to fail. It fails silently by default; see
# backend/libs/otel's diag logging setup for how to see it fail instead.
# OTEL_SERVICE_NAME=auth
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
